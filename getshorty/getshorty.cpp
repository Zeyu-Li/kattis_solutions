#include <bits/stdc++.h>

typedef long long ll;
using namespace std;

int main() {
    while (1)
    {
        int n, m;
        cin >> n >> m;

        if (n == 0 && m == 0) {
            return 0;
        }

        // TODO: graph problem
        while (m--) {
            ;
        }
    }
    
}
