#include <bits/stdc++.h>

typedef long double ld;
using namespace std;

int main() {
    ld a, b, c;
    cin >> a >> b >> c;

    cout << setprecision(18) << fixed;
    cout << a * b / c << '\n';

    return 0;
}
